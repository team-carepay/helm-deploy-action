name: Helm Deploy
description: Updates YAML file in GitHub by updating a jsonpath entry with a new value
inputs:
  file:
    description: For example 'ken-test/applications/default/security/ussd/values.yaml'
    required: true
  value:
    description: For example `v1.2.3`
    required: true
  username:
    description: Username for login to Git URL
    required: true
    default: carepaybot
  password:
    description: GitHub Personal Access Token (with repo contents write access)
    required: true
  jsonpath:
    required: true
    default: $.microservice.image
  workspace:
    description: GitHub owner/organisation
    required: true
    default: carepaydev
  repository:
    description: GitHub repository name
    required: true
    default: central-configs
runs:
  using: "node24"
  main: "dist/index.js"
